# Django framework
django==5.0

# Environment variables management
python-dotenv==1.0.0

# Django cache utilities
django-cache-memoize==0.1.10

# Optional database drivers (uncomment if needed)
# PostgreSQL support
# psycopg2-binary==2.9.7

# MySQL support  
# mysqlclient==2.2.0

# Production server (uncomment for production)
# gunicorn==21.2.0

# Optional: Enhanced archive support (currently not used in code)
# rarfile==4.0
# py7zr==0.21.0
